import axios from 'axios';

// eslint-disable-next-line react-refresh/only-export-components
export default axios.create({
  // baseURL: 'http://localhost:5050',
  baseURL: 'https://onlinegallery-be.onrender.com',
});
